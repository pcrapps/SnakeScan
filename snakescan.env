# SnakeScan environment configuration
# Copy to /etc/snakescan/snakescan.env and edit as needed

# Enable minimal driving mode UI (auto-redirects / to /drive)
DRIVING_MODE=false

# WiFi hotspot SSID (used by hotspot_setup.sh)
HOTSPOT_SSID=SnakeScan
