# Core runtime
Flask>=2.3,<3.0
numpy>=1.24,<3.0

# AI signal classification (spectrogram generation)
matplotlib>=3.7
scipy>=1.10
Pillow>=10.0

# APRS decode
aprs>=1.1

# Test-only (optional for deployment)
pytest>=7.0,<9.0

# Optional: RTL-SDR Python bindings (not required for web UI)
# pyrtlsdr>=0.3
